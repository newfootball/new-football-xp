import { Layout } from "@/components/layout";

export const Footer = () => {
  return (
    <footer className="container fixed inset-x-0 bottom-0 m-auto flex max-w-lg justify-between gap-1 border-t border-accent bg-background py-2">
      <Layout>Footer</Layout>
    </footer>
  );
};
